algoritmo "conversão de tempo em segundos"
// Função :
// Autor :
// Data : 10/05/2022
// Seção de Declarações 
var
horas, min, seg:real
inicio
escreval("Digite quantas horas são")
leia(horas)
escreval("Digite quantos minutis são")
leia(min)
//processamento
seg <- (horas*3600)+(min*60)
//saida
escreval("A quantidadde de segundos é",seg)

fimalgoritmo
